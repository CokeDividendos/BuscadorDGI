# Example Streamlit secrets configuration
# Copy this file to secrets.toml and add your actual credentials

# Admin user credentials (optional)
# If configured, the admin user will be auto-created on first run
# This is useful for Streamlit Cloud where the file system is ephemeral
[admin]
email = "admin@example.com"
password = "your-secure-password-here"
